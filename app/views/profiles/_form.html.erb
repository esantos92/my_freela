<%= form_for(profile) do |f| %>
  <% if profile.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile.errors.count, "error") %> prohibited this profile from being saved:</h2>

      <ul>
      <% profile.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :monthly_budget %>
    <%= f.text_field :monthly_budget %>
  </div>

  <div class="field">
    <%= f.label :value_hour %>
    <%= f.text_field :value_hour %>
  </div>

  <div class="field">
    <%= f.label :days_per_week %>
    <%= f.text_field :days_per_week %>
  </div>

  <div class="field">
    <%= f.label :hours_per_day %>
    <%= f.text_field :hours_per_day %>
  </div>

  <div class="field">
    <%= f.label :github_profile %>
    <%= f.text_area :github_profile %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
